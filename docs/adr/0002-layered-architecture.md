# ADR 0002: Шарова архітектура модульного моноліту Pizza Day

## Контекст
Проєкт "Pizza Day" використовує модульний моноліт з чітким розділенням відповідальностей між шарами. Необхідно визначити структуру шарів, напрямок залежностей та заборонені зв'язки для підтримки чистоти архітектури.

## Рішення

### Структура шарів та їх відповідальності

#### 🎯 **API Layer** (`com.pizzaday.api/`)
**Відповідальність:**
- Обробка HTTP запитів та відповідей
- Мапінг DTO ↔ Domain моделі
- Базова валідація вхідних даних (синтаксична)
- Обробка помилок та трансформація у відповідні HTTP статуси
- Аутентифікація та авторизація (майбутня)

**Конкретні компоненти:**
- `PizzaController` - REST endpoints для управління піцами
- `HealthController` - ендпоінт перевірки здоров'я
- DTO класи для запитів/відповідей
- Exception handlers

#### ⚙️ **Service Layer** (`com.pizzaday.service/`)
**Відповідальність:**
- Бізнес-логіка та use cases
- Валідація бізнес-правил
- Координація доменних об'єктів
- Транзакційність операцій
- Мапінг Domain ↔ DTO

**Конкретні компоненти:**
- `PizzaService` - сервіс для операцій з піцами
- DTO для внутрішнього використання (`CreatePizzaRequest`, `UpdatePizzaRequest`, `PizzaResponse`)
- Бізнес-валідатори

#### 🏗️ **Domain Layer** (`com.pizzaday.domain/`)
**Відповідальність:**
- Доменні сутності та їх інваріанти
- Бізнес-правила та логіка
- Value objects
- Доменні exceptions

**Конкретні компоненти:**
- `Pizza` - доменна сутність піци
- `PizzaSize` - value object розміру піци
- `PizzaNotFoundException` - доменна помилка

### Напрямок залежностей
