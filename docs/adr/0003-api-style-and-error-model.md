REST API Style та Error Model

## Контекст
Для забезпечення консистентності та передбачуваності API необхідно визначити стандарти REST, формат помилок та політику HTTP статус кодів. Це дозволить клієнтам ефективно взаємодіяти з API та обробляти помилки.

## Рішення

### REST API Стиль

#### 🔗 **Resource Naming**
- **Множина для колекцій**: `/api/pizzas`, `/api/orders`
- **Сингular для конкретних ресурсів**: `/api/pizzas/{id}`
- **Кресочки замість camelCase**: `/api/special-offers`
- **Вкладжені ресурси**: `/api/orders/{orderId}/items`

#### 🛣️ **Endpoint Structure**
GET /api/pizzas # Отримати список піц
POST /api/pizzas # Створити нову піцу
GET /api/pizzas/{id} # Отримати піцу по ID
PUT /api/pizzas/{id} # Повністю оновити піцу
PATCH /api/pizzas/{id} # Частково оновити піцу
DELETE /api/pizzas/{id} # Видалити піцу

#### 📋 **HTTP Methods Semantics**
- **POST** - створення нового ресурсу (повертає 201 Created)
- **GET** - отримання ресурсу (повертає 200 OK)
- **PUT** - повна заміна ресурсу (повертає 200 OK)
- **PATCH** - часткове оновлення (повертає 200 OK)
- **DELETE** - видалення ресурсу (повертає 204 No Content)

### Формат помилок (Error Response)

#### 🚨 **Error Response Schema**
```json
{
  "error": "ValidationError",
  "code": "NAME_REQUIRED", 
  "message": "Name is required",
  "details": [
    {
      "field": "name",
      "message": "Name must be at least 2 characters long"
    }
  ],
  "timestamp": "2024-01-16T10:30:00Z",
  "path": "/api/pizzas"
}
